# skin_disease_detection
Skin Disease Detection is a deep learning project that classifies skin diseases from medical images using a Convolutional Neural Network (CNN). It includes image preprocessing, model training, and prediction to improve classification accuracy.
